name: Add to Project

on:
  issues:
    types: [opened, reopened, labeled]
  pull_request:
    types: [opened, labeled]

jobs:
  call-shared:
    uses: AzureLocal/.github/.github/workflows/reusable-add-to-project.yml@main
    with:
      id-prefix: S2DCART
      solution-option-id: 'b7769a8a'
    secrets: inherit
